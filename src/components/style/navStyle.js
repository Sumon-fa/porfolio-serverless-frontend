import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles((theme) => ({
  tabContainer: {
    marginLeft: 'auto',
  },

  tab: {
    fontFamily: 'Raleway',
    textTransform: 'none',
    color: '#fff',
  },
}));
